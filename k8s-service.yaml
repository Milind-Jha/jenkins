apiVersion: v1
kind: Service
metadata:
  name: userapp-service
  namespace: encryption-app-namespace
spec:
  selector:
    app: spring-boot-k8s
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 8282
  type: NodePort